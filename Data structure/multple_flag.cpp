#include <iostream>

/*using namespace std ;

int main()
{
    int arr[5] = { 1, 5, 1, 5, 3 };
    int input[3] = { 1, 2, 3 };

    for (size_t y = 0; y < 3; y++)
    {
        int flag = 0;
        for (size_t x = 0; x < 5; x++)
        {
            if (arr[x] == input[y])
            {
                flag = 1;
                break;
            }
        }

        if (flag == 1)
        {
            std::cout << "존재 O";
        }
        else
        {
            std::cout << "존재 X";
        }

    }
    

return 0;
}*/